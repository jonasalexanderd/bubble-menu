import {StyleSheet} from 'react-native';

const bubbleMenuStyle = StyleSheet.create({
  container: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    width: '100%',
    paddingHorizontal: 10,
    alignItems: 'center',
    flex: 1,
    alignContent: 'center',
  },
  content: {
    width: '100%',
    height: '100%',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
export default bubbleMenuStyle;
